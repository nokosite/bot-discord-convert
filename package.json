{
  "name": "github-webhook-vercel",
  "version": "1.0.0",
  "description": "GitHub webhook handler on Vercel",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}
